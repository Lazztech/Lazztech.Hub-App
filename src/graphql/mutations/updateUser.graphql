mutation updateUser($data: UpdateUserInput, $image: Upload) {
  updateUser(data: $data, image: $image) {
    id
    firstName
    lastName
    description
    image
    email
    phoneNumber
    shareableId
  }
}
