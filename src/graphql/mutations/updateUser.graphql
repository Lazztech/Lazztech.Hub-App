mutation updateUser($data: UpdateUserInput, $imageFile: Upload) {
  updateUser(data: $data, imageFile: $imageFile) {
    id
    firstName
    lastName
    description
    image
    email
    phoneNumber
    shareableId
  }
}
