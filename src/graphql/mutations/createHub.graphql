mutation createHub(
    $name: String!
    $description: String
    $latitude: Float!
    $longitude: Float!
    $locationLabel: String
    $file: Upload
){
    createHub(
        name: $name, 
        description: $description,
        latitude: $latitude,
        longitude: $longitude
        locationLabel: $locationLabel
        file: $file
        ) {
            userId
            hubId
            isOwner
            starred
            isPresent
            hub {
                id
                name
                description
                active
                image
                latitude
                longitude
                usersConnection {
                    isPresent
                    isOwner
                }
            }
        }
}